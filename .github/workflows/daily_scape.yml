# ... (This is line 33 in your original file,
#      which is hidden by the '...' in your error image)

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add nba_team_stats.csv # Ensure this matches your script's output
          git commit -m "Daily Team Stats CSV Update" || echo "No changes to commit"
          git push
