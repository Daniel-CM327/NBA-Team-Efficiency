# ... (lines 1 - 33 are the same) ...
      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add nba_team_stats.csv  # <--- THIS LINE IS UPDATED
          git commit -m "Daily Team Stats CSV Update" || echo "No changes to commit"
          git push
